storage:
  folder-path: /tmp

management:
  endpoint:
    health:
      enabled: true
    shutdown:
      enabled: true
    prometheus:
      enabled: true
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        include: health, prometheus, shutdown
      base-path: /
  server:
    port: 9090

